mod logger.printer

class HelloWorld {
    pub x: int = 100
    priv y: int = 100
    prot z: int = 100
    prot zz: int = 100

}

class HiWorld: HelloWorld {
    fn t() {
        println(self.x) // work
    }
}

class To {
    fn mo() {
        println("Moo")
    }
}

fn main() {
    let m = HiWorld()
    let t = m as HelloWorld // ok

    // m should only be translated to To if
    // its a To
    let t2 = m as To // not ok, should error
    t2.mo() // should fail
}